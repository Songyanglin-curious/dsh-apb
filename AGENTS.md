# 项目规则

- 开始项目工作前，先读取 `docs/index.md`。
- `docs/` 及其每级子目录必须包含 `index.md`；新增文档目录时同时创建其索引。
- 行为、安装方式或包结构发生变化时，同步更新对应文档；不得把设计目标写成已实现事实。
- 新发现但未修复的问题记录到 `docs/project/known-issues.md`，计划项同步到
  `docs/project/roadmap.md`。
- 验证结果必须区分“已通过”“未执行”和“受阻”，端到端行为未经验证时不得宣称可用。
